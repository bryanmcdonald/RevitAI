# P2-08.7: System Prompt + Documentation

**Goal**: Add drafting guidance to the system prompt and update all project documentation to reflect the full P2-08 toolset.

**Prerequisites**: P2-08.1 through P2-08.6 complete.

**Status**: Complete

---

## Changes Required

### 1. ContextEngine.cs — System Prompt Updates

Add drafting guidance section to the system prompt when drafting tools are available:

```
## Drafting & Documentation
- Use get_fill_patterns, get_line_styles, get_detail_components to discover available resources before placing elements
- All 2D drafting tools require a detail or drafting view (not 3D, not schedule, not sheet)
- Coordinates are in feet; angles are in degrees
- For complex drawings, use batch tools (batch_place_detail_lines, batch_place_detail_components) to minimize round trips
- Fill patterns: use get_fill_patterns first, then reference by name in place_filled_region
- Line styles: use get_line_styles first, then reference by name in line_style parameter
- Sheets: use get_sheet_list + get_viewport_info to understand current layout before placing viewports
- Revision clouds require at least one revision to exist (use get_revision_list to check)
```

### 2. CLAUDE.md Updates
- Add `DraftingTools/` to folder structure
- Update "Current Status" section
- Update version history

### 3. README.md Updates
- Verify all 27 drafting tools appear in "Available Tools" table
- Update development status checkboxes for P2-08

### 4. docs/phase2/P2-08-drafting-tools.md
- Add implementation notes for each sub-chunk
- Document any API gotchas discovered during implementation

### 5. docs/phase2/README.md
- Update P2-08 status from "Not started" to complete

---

## Verification

1. Start a chat session and verify system prompt includes drafting guidance
2. Verify all documentation is consistent and up-to-date
3. Verify all 27 drafting tools appear in README tools table
